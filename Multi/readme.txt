multi instance scanner

Find instances of all RTTI classes which name contains set string within manually pre-set process memory limits (heap)
Developer version
Execute lib to register functions, then main - launcher
It is convinient to keep both scripts open in separate windows
Adjust settings as required